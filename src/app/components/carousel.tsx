'use client'
import {useState} from 'react'

//Create a react carousel component
const Carousel = () => {
    const [index, setIndex] = useState(0)
    const [direction, setDirection] = useState('next')
    const [interval, setInterval] = useState(5000)
    
    return{

    }
}